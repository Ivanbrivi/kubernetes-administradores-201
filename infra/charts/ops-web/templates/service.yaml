apiVersion: v1
kind: Service
metadata:
  name: {{ include "ops-web.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ops-web.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
